export interface Car {
    id: string,
    name: string,
    img_url: string,
    price: string,
    action: any;
}

export interface CarsWrapper {
    id: string,
    name: string,
    img_url: any,
    price: string,
    action: any;
}